import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.toddlergames',
	appName: 'Toddler Games',
	webDir: 'build',
	bundledWebRuntime: false
};

export default config;
